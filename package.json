{
  "name": "brewtendo-monorepo",
  "private": true,
  "scripts": {
    "build": "cd new-site && npm install && npm run build",
    "dev": "cd new-site && npm install && npm run dev",
    "start": "cd new-site && npm install && npm run start",
    "lint": "cd new-site && npm run lint"
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "packageManager": "npm@10.2.4"
}